package com.api.ovgs.repository;

import com.api.ovgs.entity.TypeTransport;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TipoTransporteRepository extends JpaRepository<TypeTransport, Integer> {
}
